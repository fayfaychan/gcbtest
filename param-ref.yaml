params:
  - name: echo
tasks:
- name: echo
  params:
  - name: echo
    value: "$(params.echo)"
  taskSpec:
    params:
    - name: echo
    steps:
    - name: test
      image: ubuntu
      script: |
        echo -n "$(params.echo)"
