apiVersion: tekton.dev/v1 # or tekton.dev/v1beta1
kind: Pipeline
spec:
  tasks:
  - name: echo
    params:
    - name: echo
      value: "$(params.echo)"
    taskSpec:
      steps:
      - image: ubuntu
        script: |
          echo -n "$(params.echo)"
